[general]
url.base = "${urlBase}"
translation.locale = "fr_FR"
timezone = "Europe/Paris"
system.locale = "af_ZA.UTF-8"
template.name = "_default"
site.title = "USVN 0.8"
site.ico = "medias/_default/images/USVN.ico"
site.logo = "medias/_default/images/USVN-logo.png"
subversion.path = "${reposRoot}"
subversion.passwd = "${reposHTPwd}"
subversion.authz = "${reposAuthz}"
subversion.url = "${reposURL}"
database.adapterName = "${dbAdapter}"
database.prefix = "usvn_"
database.options.host = "${dbHost}"
database.options.username = "${dbUser}"
database.options.password = "${dbPwd}"
database.options.dbname = "${dbDB}"
update.availableversion = "0.7.2"
version = "0.8.0"
