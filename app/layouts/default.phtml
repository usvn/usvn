<!DOCTYPE  html PUBLIC "-//W3C//DTD XHTML 1.1//EN"
	"http://www.w3.org/TR/xhtml11/DTD/xhtml11.dtd">
<html>
<? $view = $this->layout()->getView(); ?>
	<head>
		<meta http-equiv="Content-type" content="text/html; charset=utf-8">
		<title>USVN</title>
		<? /* $this->stylesheetTag('screen.css');*/ ?>
		<? /* $this->faviconTag(''); */ ?>
		<link type="text/css" rel="stylesheet" media="screen" href="<?= $this->url(array('type' => 'stylesheets', 'name' => 'new.css'), 'medias')?>" />
		<link rel="shortcut icon" href="<?= $this->url(array('type' => 'images', 'name' => 'logo_small.tiff'), 'medias') ?>" />
		<? if (isset($view->javascripts)): foreach ($view->javascripts as $js): ?>
		<? endforeach; endif; ?>
	</head>
	<body>
		<div id="header">
		  <div id="shaddow">
	  	  <div id="header-wrapper">
	    	  <div id="logo">
	    		  &nbsp;
	    		</div>
	    		<div id="main-menu">
		          <ul>
		<? foreach (array('index' => 'Accueil', 'profile' => 'Profile', 'admin' => 'Admin', 'logout' => 'Logout') as $menuLink => $menuLabel): ?>
		            <li><a href="<?= $this->url(array('controller' => $menuLink), 'default') ?>" <?= ($menuLink == $this->controller ? ' id="selected"' : '') ?>><span><?= T_($menuLabel) ?></span></a></li>
		<? endforeach ?>
		          </ul>
	    		</div>
	  		</div>
			</div>
	  </div>
		<div id="container">
		  <div id="container-wrapper">
	  	  <? if (isset($this->layout()->getView()->submenu)): ?>
	  	  <div id="sub-menu">
	  	  <ul>
	  	    <? foreach($this->layout()->getView()->submenu as $item): ?>
	  	    <li>
	  	    	<? if (isset($item['href'])): ?>
	  	    	  <a href="<?= htmlspecialchars($this->url(array('controller' => ''), 'default') . $item['href']); ?>">
	  	    	  	<?= htmlspecialchars(T_($item['label'])); ?>
	  	    	  </a>
	  	    	<? elseif (isset($item['url'])): ?>
	  	    	  <? $route = (isset($item['route']) ? $item['route'] : null); ?>
	  	    	  <a href="<?= htmlspecialchars($this->url($item['url'], $route)); ?>">
	  	    	  	<?= htmlspecialchars(T_($item['label'])); ?>
	  	    	  </a>
	  	    	<? else: ?>
	  	    	  <h3><?= T_($item['label']); ?></h3>
	  	    	<? endif ?>
	  	    </li>
	  	    <? endforeach ?>
	  	  </ul>
	  	  </div>
	  	  <? endif ?>
	  	  <div id="main">
				<?= $this->layout()->content ?>
				<div style="clear:both;"></div>
	  		</div>
	  		<div id="hspacer">
	  		  &nbsp;
	  		</div>
	  		<div id="footer">
	  		  Footer
	<? /* htmlspecialchars($this->headStyle('toto'), array('href' => '/toto')) */ ?>
	<? /* htmlspecialchars($this->headLink(array('href' => '/toto'))) */ ?>
	<? /* $this->headLink()->appendStylesheet('/toto') */ ?>
	<? /* htmlspecialchars($this->headLink()) */ ?>
	<? /* htmlspecialchars($this->url()) */ ?>
	<? /* $this->mediaStylesheet */ ?>
	<? /* join(array_keys((array)$this), '<br />') */ ?>
	  		</div>
			</div>
		</div>
	</body>
</html>
