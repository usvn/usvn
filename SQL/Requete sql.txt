#Cas utilisation 
Checkout selon un numero de version
les autres requetes decoulent de celle-ci

#SELECT 	MAX(revisions_num) , path
 FROM   	files
 WHERE  	revisions_num< "le numero de version demande"
 GROUP BY 	pathfile

#SELECT		MAX(revisions_num)
 FROM		files
 WHERE		path =''
 GROUP BY 	path;